# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ['nodejs-20_x', 'redis']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'bash start.sh'
